# My Static Website Deployment 🚀

This project demonstrates how to deploy a simple static website using **AWS S3**, **CloudFront**, and **GitHub**.

---

## 📌 Steps Followed

### 1. Prepare Website Code
- Created `index.html` and `style.css`.
- Tested locally in browser.
- Initialized Git repository:
  ```bash
  git init
  git add .
  git commit -m "Initial commit"
